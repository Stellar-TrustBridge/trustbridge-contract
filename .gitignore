# Rust / Soroban
/target/
**/*.rs.bk
Cargo.lock

# Soroban test snapshots (regenerated by `cargo test`)
/test_snapshots/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Environment and secrets
.env
.env.*
!.env.example

# Benchmark output (regenerated by `make bench-export` / `make bench-username`)
/bench-results.txt
/bench-username-normalization.txt

# Generated TypeScript bindings package (regenerated by `make bindings`)
/bindings/
node_modules/

# Deployment artifacts
/deployments/
*.wasm
!target/wasm32v1-none/release/*.wasm

# OS
.DS_Store
Thumbs.db

# Stellar CLI local config (machine-specific)
.stellar/

# Local-only issue push helper (keep for next wave; do not commit)
push_issues.sh
issues.md
issues.d/
