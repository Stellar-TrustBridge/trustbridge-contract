{
  "name": "TrustBridge Stellar Contract",
  "image": "mcr.microsoft.com/devcontainers/rust:1-bullseye",
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {}
  },
  "onCreateCommand": "sudo apt-get update && sudo apt-get install -y libdbus-1-dev libudev-dev pkg-config && rustup target add wasm32v1-none && cargo install --locked stellar-cli@26.1.0",
  "customizations": {
    "vscode": {
      "extensions": [
        "rust-lang.rust-analyzer",
        "tamasfe.even-better-toml",
        "vadimcn.vscode-lldb"
      ]
    }
  },
  "postAttachCommand": "stellar --version"
}
